# ÉTAT RÉEL — Mozouna / affiliate-platform — 2026-07-12

> Photo **vérifiée factuellement** (git + base prod), pas dans le plan.
> Portée de CETTE vérification : **module Livreurs (Lots A→G) uniquement** + présence des migrations
> 126→131 en prod. Pour le reste du système (socle, cœur argent 121-125, canaux affilié/grossiste/
> fournisseur), la photo de référence reste **`ETAT_REEL_2026-07-10.md`** — non re-vérifiée ici.

---

## MODULE LIVREURS & TRAÇABILITÉ — ✅ A→G TOUS EN PROD (vérifié 2026-07-12)

### Méthode de vérification (aucune écriture)
1. **`git log main`** → les 7 lots sont mergés `--no-ff` sur `main`.
2. **Requête LECTURE SEULE** sur `supabase_migrations.schema_migrations` en **PROD** (pooler
   `aws-1-eu-central-1.pooler.supabase.com:5432`, `backups/.db_password`, via le `psql` du container
   supabase local — **jamais le CLI, aucune écriture, un simple SELECT**).

### Résultat — les 6 lots sont en prod et live
| Lot | Sujet | Merge main | Migration prod |
|---|---|---|---|
| A | Registre + comptes livreurs + `/admin/couriers` | `28b0ca7` | **126 ✅ présente** |
| B | QR + code128 + étiquettes PDF + `/courier/scan` | `65668fc` | **127 ✅ présente** |
| C | Dashboard livreur mobile cloisonné | `0fff3bf` | sans migration |
| D | Tournées + scan ramassage + retours 3 cas | `e294897` | **128 ✅ présente** |
| E | Notifications instantanées par état (3 canaux) | `7267b41` | **129 ✅ présente** |
| F | Relevés PDF figés (affilié au payout + livreur signable) | `9344733` | **130 ✅ présente** |
| G | Agent Gardien anti-collusion (RÈGLE DU PORTEUR + double confirmation + patterns) | `730baf0` | **131 ✅ présente** |

> `SELECT version FROM supabase_migrations.schema_migrations WHERE version ~ '^(126|127|128|129|130|131)'`
> → renvoie **126, 127, 128, 129, 130, 131**. Chaîne de traçabilité complète (registre → étiquettes →
> scan livraison → dashboard cloisonné → tournées/retours → notifications → relevés PDF → **Agent
> Gardien anti-collusion**) **EN PROD**.

### Ce qui a nourri la réconciliation doc
`LIVRABLE_MODULE_LIVREURS.md` affichait « GO-ready » pour A/C/E/F puis G (retard doc vs réel) →
corrigé le 2026-07-12 : les 7 lignes passent à **EN PROD**, avec commit de merge + n° de migration.

---

## LOT G — AGENT GARDIEN ANTI-COLLUSION — ✅ EN PROD (2026-07-12)

- **Mergé main `--no-ff` `730baf0`** (commit branche `4ca9835`) + poussé. **Migration 131 APPLIQUÉE EN
  PROD** (pooler `backups/.db_password`, `--single-transaction` lockstep APRÈS Vercel Ready, jamais le CLI).
- **Vérif APRÈS en base prod** : 6 tables (guardian_alerts append-only ineffaçable, courier_blocks,
  courier_staff_pairs, courier_cash_confirmations, inventory_snapshots + _lines) + 15 RPC + 4 vues ;
  RLS activée ; **tables VIDES** ; **REVOKE effectif** (`authenticated` ne peut exécuter ni
  `confirm_cash_receipt` ni `record_depot_reception`) ; trigger d'immuabilité présent ; 0 policy d'écriture.
- **RÈGLE DU PORTEUR structurelle** : `record_depot_reception` n'accepte AUCUN courier_id d'imputation
  (porteur déduit du scan `pickup_dispatch`) ; colis fantôme / imputation croisée → refus ; réception
  sans déclaration → collusion + dette gelée. **Double confirmation argent** admin-only, 2 comptes
  distincts, zéro compensation croisée (réutilise `reconcile_courier_remittance` mig 122 INCHANGÉE).
- Audits **@finance 🟢 + @security 🟢** ; 4 checks verts ; **13 tests de fraude simulée** (les 4 fraudes
  obligatoires échouent comme voulu). Écrans `/admin/guardian` + `/admin/couriers/{reception,inventory}`
  (mobile 390×844, porteur imposé), i18n FR/AR/EN + RTL.

---

## RESTE DU SYSTÈME
Voir **`ETAT_REEL_2026-07-10.md`** (socle, cœur argent 121-125, réconciliation livreur/trésorerie P0,
canaux). Non re-vérifié dans cette session — cette photo ne le remplace pas, elle le complète sur le
seul module Livreurs.

*Dernière mise à jour : 2026-07-12 — module Livreurs A→G **100% EN PROD** : A→F confirmés (schema_migrations 126→130) + Lot G Agent Gardien livré (merge `730baf0` + mig 131 appliquée prod, vérif AVANT/APRÈS OK).*
