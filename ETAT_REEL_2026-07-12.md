# ÉTAT RÉEL — Mozouna / affiliate-platform — 2026-07-12

> Photo **vérifiée factuellement** (git + base prod), pas dans le plan.
> Portée de CETTE vérification : **module Livreurs (Lots A→F) uniquement** + présence des migrations
> 126→130 en prod. Pour le reste du système (socle, cœur argent 121-125, canaux affilié/grossiste/
> fournisseur), la photo de référence reste **`ETAT_REEL_2026-07-10.md`** — non re-vérifiée ici.

---

## MODULE LIVREURS & TRAÇABILITÉ — ✅ A→F TOUS EN PROD (vérifié 2026-07-12)

### Méthode de vérification (aucune écriture)
1. **`git log main`** → les 6 lots sont mergés `--no-ff` sur `main`.
2. **Requête LECTURE SEULE** sur `supabase_migrations.schema_migrations` en **PROD** (pooler
   `aws-1-eu-central-1.pooler.supabase.com:5432`, `backups/.db_password`, via le `psql` du container
   supabase local — **jamais le CLI, aucune écriture, un simple SELECT**).

### Résultat — les 6 lots sont en prod et live
| Lot | Sujet | Merge main | Migration prod |
|---|---|---|---|
| A | Registre + comptes livreurs + `/admin/couriers` | `28b0ca7` | **126 ✅ présente** |
| B | QR + code128 + étiquettes PDF + `/courier/scan` | `65668fc` | **127 ✅ présente** |
| C | Dashboard livreur mobile cloisonné | `0fff3bf` | sans migration |
| D | Tournées + scan ramassage + retours 3 cas | `e294897` | **128 ✅ présente** |
| E | Notifications instantanées par état (3 canaux) | `7267b41` | **129 ✅ présente** |
| F | Relevés PDF figés (affilié au payout + livreur signable) | `9344733` | **130 ✅ présente** |

> `SELECT version FROM supabase_migrations.schema_migrations WHERE version ~ '^(126|127|128|129|130)'`
> → renvoie **126, 127, 128, 129, 130**. Chaîne de traçabilité complète (registre → étiquettes →
> scan livraison → dashboard cloisonné → tournées/retours → notifications → relevés PDF) **EN PROD**.

### Ce qui a nourri la réconciliation doc
`LIVRABLE_MODULE_LIVREURS.md` affichait encore « GO-ready » pour A/C/E/F (retard doc vs réel) →
corrigé le 2026-07-12 : les 6 lignes passent à **EN PROD**, avec commit de merge + n° de migration.

---

## SEUL RESTE DU MODULE — Lot G (Agent Gardien) ⏭️

- **Non démarré. Aucune branche `feat/livreurs-lot-g`.** (`git branch` ne liste que `-a`→`-f`.)
- Spécifié et **verrouillé** dans `LIVRABLE_MODULE_LIVREURS.md` §CHAÎNE DE GARDE : surveillance
  anti-fuite (retour déclaré → scan réception = double confirmation, escalade 48h « retour fantôme »,
  alerte collusion, blocage auto perso / manuel sociétés, email récap quotidien, détection de pattern
  livreur↔salarié) + **RÈGLE DU PORTEUR** (imputation automatique au porteur du ramassage, le salarié
  ne choisit jamais le livreur à la réception, la dette ne change jamais de propriétaire).
- **Chantier financier + sécurité** → session dédiée : @architect → @finance + @security → implémentation.
  **NE PAS coder sans GO Abdou.**

---

## RESTE DU SYSTÈME
Voir **`ETAT_REEL_2026-07-10.md`** (socle, cœur argent 121-125, réconciliation livreur/trésorerie P0,
canaux). Non re-vérifié dans cette session — cette photo ne le remplace pas, elle le complète sur le
seul module Livreurs.

*Dernière mise à jour : 2026-07-12 — réconciliation doc↔réel, module Livreurs A→F confirmé EN PROD (git + schema_migrations prod, lecture seule).*
