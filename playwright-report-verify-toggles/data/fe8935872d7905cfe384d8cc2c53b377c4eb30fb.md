# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: verify-toggles-mobile.spec.ts >> D — rollback UI + message erreur si serveur refuse
- Location: e2e/verify-toggles-mobile.spec.ts:266:5

# Error details

```
TimeoutError: locator.textContent: Timeout 20000ms exceeded.
Call log:
  - waiting for locator('button[type="button"].rounded-lg').first()

```

# Page snapshot

```yaml
- 'heading "Application error: a client-side exception has occurred while loading localhost (see the browser console for more information)." [level=2] [ref=e4]'
```

# Test source

```ts
  208 |   const client = await ctx.newCDPSession(page)
  209 |   await client.send('Network.emulateNetworkConditions', {
  210 |     offline: false,
  211 |     downloadThroughput: 500 * 1024 / 8,  // 500 KB/s
  212 |     uploadThroughput: 250 * 1024 / 8,    // 250 KB/s
  213 |     latency: 2000,                         // 2s latence
  214 |   })
  215 | 
  216 |   await shot(page, 'C-throttle-before-click.png')
  217 | 
  218 |   const toggleBtn = toggleBtns.first()
  219 |   const initialClass = await toggleBtn.getAttribute('class')
  220 |   const initialText = await toggleBtn.textContent()
  221 |   console.log(`[C] Before click: text="${initialText?.trim()}", throttle=Slow3G+2s`)
  222 | 
  223 |   const t0 = Date.now()
  224 |   await toggleBtn.click()
  225 |   const t1 = Date.now()
  226 | 
  227 |   // Capture < 200ms après le clic — état optimiste visible
  228 |   await page.waitForTimeout(100)
  229 |   await shot(page, 'C-throttle-instant.png')
  230 | 
  231 |   const afterClass = await toggleBtn.getAttribute('class')
  232 |   const afterText = await toggleBtn.textContent()
  233 |   const elapsed = t1 - t0
  234 | 
  235 |   console.log(`[C] After click (${elapsed}ms delay to get handle): text="${afterText?.trim()}"`)
  236 |   console.log(`[C] Class before: ${initialClass?.slice(0, 60)}`)
  237 |   console.log(`[C] Class after:  ${afterClass?.slice(0, 60)}`)
  238 | 
  239 |   // Le bouton NE DOIT PAS être disabled (isPending → opacity-60 mais pas disabled)
  240 |   const isDisabled = await toggleBtn.isDisabled()
  241 |   console.log(`[C] isDisabled after click: ${isDisabled}`)
  242 |   expect(isDisabled).toBe(false)
  243 | 
  244 |   // L'état doit avoir changé visuellement (classe ou texte différent)
  245 |   const changed = afterClass !== initialClass || afterText?.trim() !== initialText?.trim()
  246 |   console.log(`[C] Visual state changed immediately: ${changed}`)
  247 |   expect(changed).toBe(true)
  248 | 
  249 |   // Attendre fin du round-trip sous throttle
  250 |   await page.waitForTimeout(10_000)
  251 |   await shot(page, 'C-throttle-after-roundtrip.png')
  252 | 
  253 |   // Désactiver throttling
  254 |   await client.send('Network.emulateNetworkConditions', {
  255 |     offline: false,
  256 |     downloadThroughput: -1,
  257 |     uploadThroughput: -1,
  258 |     latency: 0,
  259 |   })
  260 | 
  261 |   await ctx.close()
  262 | })
  263 | 
  264 | // ─── D: Rollback sur refus serveur ───────────────────────────────────────────
  265 | 
  266 | test('D — rollback UI + message erreur si serveur refuse', async ({ browser }) => {
  267 |   const ctx = await browser.newContext({ viewport: MOBILE_VIEWPORT })
  268 |   const page = await ctx.newPage()
  269 |   await loginAdmin(page)
  270 | 
  271 |   await page.goto(`${BASE}/admin/permissions`)
  272 |   await page.waitForSelector('main', { timeout: 15_000 })
  273 | 
  274 |   const toggleBtns = toggleBtnLocator(page)
  275 |   const count = await toggleBtns.count()
  276 |   if (count === 0) {
  277 |     test.skip(true, 'Aucun bouton toggle sur /admin/permissions — scénario D non applicable')
  278 |     return
  279 |   }
  280 | 
  281 |   const toggleBtn = toggleBtns.first()
  282 |   const initialClass = await toggleBtn.getAttribute('class')
  283 |   const initialText = await toggleBtn.textContent()
  284 |   console.log(`[D] Initial: text="${initialText?.trim()}"`)
  285 | 
  286 |   // Intercepter UNIQUEMENT les requêtes avec header Next-Action (server actions)
  287 |   // sans bloquer les navigations ou ressources statiques
  288 |   await page.route('**', async (route) => {
  289 |     const req = route.request()
  290 |     const headers = req.headers()
  291 |     if (req.method() === 'POST' && headers['next-action']) {
  292 |       console.log(`[D] Aborting server action: ${req.url()} (Next-Action: ${headers['next-action']?.slice(0, 20)})`)
  293 |       await route.abort('failed')
  294 |     } else {
  295 |       await route.continue()
  296 |     }
  297 |   })
  298 | 
  299 |   await shot(page, 'D-before-click.png')
  300 | 
  301 |   // Cliquer — optimistic flip PUIS rollback attendu
  302 |   await toggleBtn.click()
  303 | 
  304 |   // Capture < 300ms — doit montrer l'état optimiste (basculé)
  305 |   await page.waitForTimeout(200)
  306 |   await shot(page, 'D-rollback-optimistic-flip.png')
  307 | 
> 308 |   const textAfterClick = await toggleBtn.textContent()
      |                                          ^ TimeoutError: locator.textContent: Timeout 20000ms exceeded.
  309 |   const classAfterClick = await toggleBtn.getAttribute('class')
  310 |   console.log(`[D] After click (t≈200ms): text="${textAfterClick?.trim()}"`)
  311 | 
  312 |   const optimisticFlipped = textAfterClick?.trim() !== initialText?.trim() || classAfterClick !== initialClass
  313 |   console.log(`[D] Optimistic flip visible: ${optimisticFlipped}`)
  314 | 
  315 |   // Attendre le rollback — l'action échoue (abortée) → rollback + erreur
  316 |   // startTransition → on attend que la transition se termine (~fetch timeout)
  317 |   await page.waitForTimeout(8_000)
  318 | 
  319 |   await shot(page, 'D-rollback-error.png')
  320 | 
  321 |   const textAfterRollback = await toggleBtn.textContent()
  322 |   const classAfterRollback = await toggleBtn.getAttribute('class')
  323 |   console.log(`[D] After rollback: text="${textAfterRollback?.trim()}"`)
  324 | 
  325 |   // Vérifier que le message d'erreur [role=alert] s'affiche
  326 |   const errorAlert = page.locator('[role="alert"]')
  327 |   const alertCount = await errorAlert.count()
  328 |   console.log(`[D] Error alerts visible: ${alertCount}`)
  329 | 
  330 |   if (alertCount > 0) {
  331 |     const errorText = await errorAlert.first().textContent()
  332 |     console.log(`[D] Error message: "${errorText?.trim()}"`)
  333 |     expect(errorText?.trim().length ?? 0).toBeGreaterThan(0)
  334 |     console.log(`[D] ROLLBACK + erreur = OK`)
  335 |   } else {
  336 |     // Si le fetch abort ne passe pas par le catch de startTransition, documenter
  337 |     // (Next.js server actions via fetch — abort → TypeError thrown dans startTransition → peut ou non être catchable)
  338 |     console.warn('[D] WARNING: Aucun [role=alert] après abort — le rollback peut ne pas se déclencher sur abort réseau')
  339 |     // Vérifier au moins que l'état n'est PAS resté "basculé" sans confirmation
  340 |     // (pour les cas où l'erreur est silencieuse mais l'état rollback quand même)
  341 |     const stateRolledBack = textAfterRollback?.trim() === initialText?.trim()
  342 |     console.log(`[D] State rolled back to initial: ${stateRolledBack}`)
  343 |     // On ne fail pas sur l'absence d'alerte si le rollback a eu lieu — les deux sont acceptables
  344 |     // mais on le note comme FINDING
  345 |   }
  346 | 
  347 |   await page.unroute('**')
  348 |   await ctx.close()
  349 | })
  350 | 
  351 | // ─── E: Overflow tableau audit 390px — scroll horizontal ─────────────────────
  352 | 
  353 | test('E — tableau audit scrollable horizontalement à 390px', async ({ browser }) => {
  354 |   const ctx = await browser.newContext({ viewport: MOBILE_VIEWPORT })
  355 |   const page = await ctx.newPage()
  356 |   await loginAdmin(page)
  357 | 
  358 |   await page.goto(`${BASE}/admin/permissions`)
  359 |   await page.waitForSelector('main', { timeout: 15_000 })
  360 | 
  361 |   // Attendre que la section audit soit chargée (table ou message vide)
  362 |   await page.waitForTimeout(1_000)
  363 |   await shot(page, 'E-audit-scroll-390-before.png')
  364 | 
  365 |   // Vérifier le conteneur overflow-x-auto (le fix du tableau)
  366 |   const scrollContainer = page.locator('.overflow-x-auto').first()
  367 |   const containerExists = await scrollContainer.count()
  368 |   expect(containerExists).toBeGreaterThan(0)
  369 |   console.log(`[E] overflow-x-auto container found: ${containerExists > 0}`)
  370 | 
  371 |   // Vérifier que la table a min-w-[640px]
  372 |   const table = page.locator('.overflow-x-auto table').first()
  373 |   const tableCount = await table.count()
  374 | 
  375 |   if (tableCount > 0) {
  376 |     const minWidth = await table.evaluate((el) =>
  377 |       window.getComputedStyle(el).minWidth
  378 |     )
  379 |     console.log(`[E] table computed min-width: "${minWidth}"`)
  380 |     // Doit être 640px
  381 |     expect(minWidth).toBe('640px')
  382 | 
  383 |     // Le conteneur doit être scrollable (scrollWidth > clientWidth à 390px)
  384 |     const scrollable = await scrollContainer.evaluate((el) => ({
  385 |       scrollWidth: el.scrollWidth,
  386 |       clientWidth: el.clientWidth,
  387 |       isScrollable: el.scrollWidth > el.clientWidth,
  388 |     }))
  389 |     console.log(`[E] scrollWidth=${scrollable.scrollWidth}, clientWidth=${scrollable.clientWidth}, scrollable=${scrollable.isScrollable}`)
  390 |     expect(scrollable.isScrollable).toBe(true)
  391 | 
  392 |     // Scrolle jusqu'à la fin pour voir les colonnes Date/Actor
  393 |     await scrollContainer.evaluate((el) => { el.scrollLeft = el.scrollWidth })
  394 |     await page.waitForTimeout(300)
  395 |     await shot(page, 'E-audit-scroll-390-after.png')
  396 | 
  397 |     // Capture scroll partiel (mi-chemin) pour montrer le scroll fonctionne
  398 |     await scrollContainer.evaluate((el) => { el.scrollLeft = el.scrollWidth / 2 })
  399 |     await page.waitForTimeout(300)
  400 |     await shot(page, 'E-audit-scroll-390-mid.png')
  401 | 
  402 |     // Vérifier que les coins arrondis du conteneur parent (rounded-xl) sont préservés
  403 |     const outerBox = page.locator('.rounded-xl.overflow-hidden').first()
  404 |     const outerCount = await outerBox.count()
  405 |     console.log(`[E] rounded-xl overflow-hidden wrapper: ${outerCount > 0}`)
  406 |     expect(outerCount).toBeGreaterThan(0)
  407 | 
  408 |   } else {
```